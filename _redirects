# Redirections pour Cloudflare Pages.
#
# La redirection www -> non-www ne peut pas se faire ici (Cloudflare
# n'accepte que des chemins relatifs dans ce fichier, pas des domaines
# complets). Elle est configurée séparément via une Redirect Rule
# dans le dashboard Cloudflare (Rules > Redirect Rules).
